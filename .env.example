# Server Configuration
MCP_SERVER_PORT=3001      # The port the server will listen on
MCP_SERVER_HOST=0.0.0.0  # The host address to bind to

# Security
SHARED_ACCESS_TOKEN=      # Optional: Set this to require authentication for uploads

# Storage Configuration
PRIVATE_KEY=              # Required: Your Storacha private key for storage access
DELEGATION=               # Required: Your storage delegation
GATEWAY_URL=https://storacha.link  # Optional: Custom gateway URL for file retrieval

# File Limits
MAX_FILE_SIZE=104857600   # Maximum file size in bytes (default: 100MB) 